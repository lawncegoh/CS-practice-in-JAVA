public static boolean isPalindrome(String word) {
		
		String a = "";
		int len = word.length();

		for(int i = len-1; i >= 0; i--) {
			a = a + word.charAt(i);  
		}	

		if(a.equalsIgnoreCase(word)) {
			return true;
		} else {
			return false;
		}
	}
